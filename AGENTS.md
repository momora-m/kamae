# kamae

Windows 向けアクションゲームエンジン。まだ初期で、実行ファイルは `editor` ひとつ。ゲームループ、エンティティ、物理の層はない。このファイルに無い構造を、先回りして作らない。

## 先に読む

- `docs/adr/0001-dx11-imgui-editor-slice.md` — 今の描画とエディタの範囲
- `docs/adr/0002-renderer-owns-directx.md` — DirectX は Renderer の中
- `docs/adr/0003-cube-walks-on-ground.md` — 床。歩行の向きは 0004 が置き換えた
- `docs/adr/0004-cube-walks-its-facing.md` — 立方体は自分のヨーで歩く。既定
- `docs/adr/0005-walk-along-camera-yaw.md` — 切り替えるとカメラの水平ヨーで歩く
- `docs/adr/0006-walk-stops-at-floor-edge.md` — 歩行は床の端で止まる。落ちない。クランプは 0008 が置き換えた
- `docs/adr/0007-player-and-opponent-one-loop.md` — プレイヤーと相手は同じ並び。三人目は要素
- `docs/adr/0008-walk-stops-before-wall-and-subject.md` — 歩行は壁と他の主体の手前で止まる
- `docs/adr/0009-attack-reduces-remaining.md` — 攻撃が届くと残りが減る。尽きた主体は描かない
- `docs/adr/0010-attack-box-one-frame.md` — 判定と同じ箱を一色で描く。出したフレームだけは 0019 が置き換えた
- `docs/adr/0011-opponent-same-attack.md` — 相手は同じ攻撃でプレイヤーを倒す
- `docs/adr/0012-edit-subject-pose.md` — パネルで全主体の位置とヨーを編集する
- `docs/adr/0013-add-remove-opponents.md` — パネルで相手を 0 人から 3 人まで増減する
- `docs/adr/0014-save-arena-layout.md` — 初期配置を arena.txt に保存し、起動時に読み戻す
- `docs/adr/0015-edit-then-start-a-trial.md` — 編集と試走を分け、名前付きの配置から始める
- `docs/adr/0016-winnable-trial-attack.md` — 先行入力と相手の初撃の待ち。間隔はプレイヤー 0.4 秒、相手 0.8 秒
- `docs/adr/0017-actions-reach-subjects.md` — 試走中、主体は水平の歩きと攻撃する／しないだけを受け取る
- `docs/adr/0018-opponent-state-is-actions.md` — 相手の状態は、その歩きと攻撃する／しないの組み合わせ。最初は近づく、間合いで待つ、撃つ
- `docs/adr/0019-attack-volume-lifetime.md` — 攻撃の箱は 3 フレームの寿命。出始めと終わりは 0。判定と描画は同じ区間
- `docs/adr/0020-subject-frame-dt.md` — 試走中、主体ごとにフレームの Δt。当たった主体は次の 4 フレームが 0。箱の寿命とは別
- `docs/adr/0021-controller-attaches-to-subject.md` — コントローラーは主体に付く。プレイヤーのキーと相手の接近が、同じ歩きと攻撃する／しないを出す
- `docs/adr/0022-session-owns-volumes.md` — 攻撃の箱は試走が持つ並びへ出る。寿命は 0019 のまま
- `docs/adr/0023-hit-is-session-result.md` — 当たりは試走が適用する結果。箱は重なりだけを報告する
- `docs/adr/README.md` — ADR の書き方
- `README.md` — Windows でのビルド

判断の本文は ADR に書く。ここには手順と禁止だけを書く。

## 今の範囲

- C++20、CMake 3.24 以上、実行ファイル名は `editor`
- Win32、DirectX 11、Dear ImGui（docking）、HLSL は `D3DCompile`
- 描くものは、残りがある主体の立方体と、その下の床一枚、床の縁の壁である。色は主体ごとに一色で、面ごとの色はない。壁は別の一色である。床の半辺は 4 から 40 で、壁はその縁から作る。攻撃の箱は試走が持つ並びに出る。出したフレームから 3 フレーム、判定と同じ箱を主体とは別の一色で描く。出始めと終わりは 0 フレームである。同じ箱は一つの主体の残りを一度だけ減らす。主体の槽には戻さない。透視カメラ。ビューのドラッグでプレイヤーの周りを回る
- ImGui で変えるのはクリア色、各主体の位置、先頭のピッチとヨーとロール、それ以外の主体のヨー、相手の増減、床の半辺、名前付きシーンの保存と読み込み、試走の開始、止めたあとに開始状態へ戻すこと、カメラ距離、歩行をカメラのヨーに合わせる切り替え。残りは読むだけで、パネルからは変えない。起動したときは編集で、時間は止まっている。編集中は位置、ヨー、相手の増減、床の半辺を変えられる。その変更は時間ではない。試走は Start を押したときだけ始まり、試走中は配置を編集する操作を出さない。試走中はシーンを切り替えない。編集中は名前を付けてシーンを複数保存し、一覧から選んで読み込める。Start を押したときの配置を開始状態として覚える。保存していない変更も入る。全滅で止めたあとの戻す操作も、試走中には出さない。一つのシーンは作業ディレクトリの `scenes` にあり、中身は `floor` と床の半辺、主体の位置、ヨー、色である。残り、クールダウン、ピッチ、ロール、カメラ、クリア色は書かない。読み込んだ主体の残りは 3、クールダウンは 0、ピッチとロールは 0 である。行が壊れている、数が範囲外、主体が 0 人または 5 人以上のときは読み込まず、今の配置を保って失敗を出す。起動時に `arena.txt` は読まない。既にある `arena.txt` は消さない。保存は、コードの 1 対 1 の初期配置を上書きしない。編集中の初期状態に戻す操作は、今の配置をその 1 対 1 に置き換える。プレイヤーは `(0, 0, 0)`、ヨー 0 度、色 `(0.78, 0.48, 0.27)`。相手は `(0, 0, 4)`、ヨー 180 度、色 `(0.25, 0.42, 0.68)`。床の半辺は 20。この位置では攻撃の箱は届かない。保存してあるシーンは消さない。試走中はこの操作を出さない。ヨーをドラッグしている相手は、そのフレームだけ接近がヨーを置き換えない。相手は 0 人から 3 人までで、消せるのは末尾だけである
- 主体は長さ 4 の並びで、使う人数だけを変える。初期は 2 人である。先頭がプレイヤーで、パネルから消せない。足した相手の位置は `(0, 0, 4)`、ヨーは 180 度、色は `(0.25, 0.42, 0.68)`、残りは 3 である。重なっていても、その場では解消しない。各主体の残りは 3 から始まる。0 の主体は描かず、歩行の障害にも攻撃の対象にもしない。歩きと攻撃は試走中だけである。先頭以外はプレイヤーの XZ へ秒あたり 2.5 で歩き、移動中のヨーはその方向を向く。攻撃の箱がプレイヤーに重なる距離では立ち止まり、重なってから 0.5 秒待って最初の攻撃を出す。範囲を外れると待ちは消え、次に入ったときまた 0.5 秒待つ。居続けるあいだの繰り返しは 0.8 秒に一度である。開始を押したとき、その配置を開始状態として覚える。各主体の残りは 3、クールダウンは 0 から試走を始める。戦闘の途中経過は覚えない。プレイヤーの残りが 0 になると試走を止める。使っている相手が 1 人以上いて、その全員の残りが 0 のときも止める。相手が 0 人のときは、相手の全滅では止めない。止めたあとは自動では開始状態に戻さない。残りが 0 の主体は描かない。戻す操作で開始状態の配置に戻り、編集に戻る。試走は自動では再開しない。一部の相手だけが 0 のときは、その相手は止まり、試走は続き、プレイヤーは歩ける。相手が 0 人でも、プレイヤーの残りがあるあいだは歩ける
- プレイヤーのキーと相手の接近は、主体に付いたコントローラーである。どちらも水平の歩きと、攻撃する／しないを出す。試走はその操作から歩きと攻撃を進める。歩きと攻撃は先頭の添字を特別扱いしない。カメラの注視点と全滅の判定は、先頭の主体のままである
- 箱の重なりは当たりの結果を出す。残りと残フレームは箱の中では変えない。試走が残りを 1 減らし、当たった主体の Δt を 0 にする
- 試走中、ビューポートにカーソルがあるとき、WASD でプレイヤーが XZ を歩く。既定の前後左右はプレイヤーのヨーで、歩行はヨーを上書きしない。切り替えるとカメラの水平ヨーになり、移動中だけヨーが移動方向を向く。動いたあとに、壁か他の主体へ重なった軸の移動だけ取り消す。相手の歩行も同じ解消である。Y は変えない。Space でプレイヤーが自分のヨーの前方を一度攻撃する。次まで 0.4 秒空ける。クールダウンの最後の 0.15 秒に押した Space は覚え、0 になったフレームに一度出す。それより前の Space は捨てる。届くと他の主体の残りが 1 減る。歩きながらの接触では減らない。出したフレームから 3 フレーム、判定に使った箱を描き、そのあいだ判定も残る。出始めと終わりは 0 フレームである。空振りでも描く。同じ箱は一つの主体を一度だけ減らす。覚えた押下のフレームには箱を出さない。クールダウンが残っているフレームには新しい箱は出ない。プレイヤーの残りが 0 のあいだは、WASD も Space も効かない。編集中と、試走を止めたあとは、WASD も Space も相手の歩行も攻撃も進まない。出し済みの箱は、試走を止めたあとも残フレームのあいだ描く。編集に戻ると消す。試走中、当たった主体は次のフレームから 4 フレーム、歩きと攻撃に渡す秒が 0 になる。キーは毎フレーム読む。当たっていない主体は今の経過秒のまま進む。箱の寿命は止まらない。クールダウンが既に 0 のときは、0 秒でも攻撃するは届く

次を入れるときは、先に ADR を `accepted` にする。それまではコードに入れない。

- DirectX 12、描画バックエンドの抽象化
- ECS、シーン階層、コンポーネント
- 物理、アニメーション、音声
- アセットパイプライン、独自 GUI

## 変更の手順

コミットしてよいか、push してよいか、PR を作ってよいかは聞かない。次の順で進める。

1. 作業する。機能や設計の変更は GitHub issue から始める。issue に書いていないものを足さない。描画 API、依存ライブラリ、ディレクトリ構成、ゲームの仕組みを変えるときは、コードの前に `docs/adr/` の ADR を `accepted` にする。ファイル名は `NNNN-kebab-title.md`。状態は `proposed` / `accepted` / `superseded`。本文は日本語。実装は、その issue と ADR の範囲だけ。
2. ブランチを切る。`main` には直接コミットしない。作業ツリーの変更を持ったまま、`cursor/` で始まる名前のブランチを作る。すでにそのブランチにいるときは、新しく切らない。
3. コミットして push する。メッセージは日本語で、なぜ変えたかが分かる一文にする。push 先は今のブランチで、`main` へは push しない。`--force` は使わない。
4. `main` 向けの PR を作る。説明は日本語で、これまでの PR に合わせ、概要、変更、確認の順に書く。概要では issue を示す。何を変えたか、Windows でどうビルドするか、この環境で実行していないならその旨を書く。issue がない文書だけの変更は、概要にその旨を書く。

## ビルド

Visual Studio 2022 と Windows 10 SDK がある Windows だけでビルドする。手順は README に従う。

Linux やクラウド VM には Windows SDK がない。そこでは構成の確認までに留め、ビルドや起動に成功したと書かない。

## コード

- `src/` — ウィンドウ、ImGui、レンダラ
- `shaders/` — HLSL
- 新しいトップレベルディレクトリは、ADR か issue が場所を決めてから作る
- 見た目とファイルの分け方は、隣のソースに合わせる。このファイルでコーディング規約を新設しない

## 言葉

ADR、issue、PR、README は日本語。識別子と CMake のターゲット名は英語のまま。
