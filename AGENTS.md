# kamae

Windows 向けアクションゲームエンジン。まだ初期で、実行ファイルは `editor` ひとつ。ゲームループ、エンティティ、物理の層はない。このファイルに無い構造を、先回りして作らない。

## 先に読む

- `docs/words.md` — 文書で使う言葉
- `docs/adr/0001-dx11-imgui-editor-slice.md` — 最初の画面は DirectX 11 と Dear ImGui
- `docs/adr/0002-renderer-owns-directx.md` — DirectX は `Renderer` の中
- `docs/adr/0003-cube-walks-on-ground.md` — 床の上を歩く。向きは 0004 が置き換えた
- `docs/adr/0004-cube-walks-its-facing.md` — 既定は自分のヨーで歩く
- `docs/adr/0005-walk-along-camera-yaw.md` — 切り替えるとカメラの水平ヨーで歩く
- `docs/adr/0006-walk-stops-at-floor-edge.md` — 床から落ちない。端の固定は 0008 が置き換えた
- `docs/adr/0007-player-and-opponent-one-loop.md` — プレイヤーと相手は同じ並び
- `docs/adr/0008-walk-stops-before-wall-and-subject.md` — 壁と他のキャラクターの手前で止まる
- `docs/adr/0009-attack-reduces-remaining.md` — 攻撃が届くと体力が減る
- `docs/adr/0010-attack-box-one-frame.md` — 判定と同じヒットボックスを描く。1 フレームだけは 0019 が置き換えた
- `docs/adr/0011-opponent-same-attack.md` — 相手は同じ攻撃でプレイヤーを倒す
- `docs/adr/0012-edit-subject-pose.md` — パネルで位置とヨーを編集する
- `docs/adr/0013-add-remove-opponents.md` — 相手は 0 人から 3 人
- `docs/adr/0014-save-arena-layout.md` — 配置ファイルの行。`arena.txt` 一つと起動時読みは 0015 が置き換えた
- `docs/adr/0015-edit-then-start-a-trial.md` — 編集モードとプレイモード。名前付きシーンから始める
- `docs/adr/0016-winnable-trial-attack.md` — プレイヤーが先に当てられる間隔
- `docs/adr/0017-actions-reach-subjects.md` — 入力は歩きと攻撃になってからキャラクターに届く
- `docs/adr/0018-opponent-state-is-actions.md` — 相手の状態は、歩きと攻撃する／しないの組み合わせである
- `docs/adr/0019-attack-volume-lifetime.md` — ヒットボックスは寿命を持ち、判定と描画が同じ区間を見る
- `docs/adr/0020-subject-frame-dt.md` — プレイ中、キャラクターごとに更新経過時間を渡す
- `docs/adr/0021-controller-attaches-to-subject.md` — コントローラーはキャラクターに付き、同じ入力を出す
- `docs/adr/0022-session-owns-volumes.md` — ヒットボックスはプレイセッションが持つ配列で管理する
- `docs/adr/0023-hit-is-session-result.md` — ヒットはプレイセッションが適用する結果である
- `docs/adr/README.md` — ADR の書き方と一覧
- `README.md` — Windows でのビルド

判断の本文は ADR に書く。ここには手順と禁止だけを書く。

## 今の範囲

- C++20、CMake 3.24 以上、実行ファイル名は `editor`
- Win32、DirectX 11、Dear ImGui（docking）、HLSL は `D3DCompile`
- 描くものは、体力があるキャラクターの立方体と、その下の床一枚、床の縁の壁である。色はキャラクターごとに一色で、面ごとの色はない。壁は別の一色である。床の半辺は 4 から 40 で、壁はその縁から作る。攻撃のヒットボックスはプレイセッションが持つ配列に出る。出したフレームから 3 フレーム（発生 0F・持続 3F・硬直 0F）、判定と同じヒットボックスをキャラクターとは別の一色で描く。同じヒットボックスは一つのキャラクターの体力を一度だけ減らす。キャラクターごとのスロットには戻さない。透視カメラ。ビューのドラッグでプレイヤーの周りを回る
- ImGui で変えるのはクリア色、各キャラクターの位置、先頭のピッチとヨーとロール、それ以外のキャラクターのヨー、相手の増減、床の半辺、名前付きシーンの保存と読み込み、プレイの開始、止めたあとに開始状態へ戻すこと、カメラ距離、歩行をカメラのヨーに合わせる切り替え。体力は読むだけで、パネルからは変えない。起動したときは編集モードで、時間は止まっている。編集中は位置、ヨー、相手の増減、床の半辺を変えられる。その変更は時間ではない。プレイモードは Start を押したときだけ始まり、プレイ中は配置を編集する操作を出さない。プレイ中はシーンを切り替えない。編集中は名前を付けてシーンを複数保存し、一覧から選んで読み込める。Start を押したときの配置を開始状態として覚える。保存していない変更も入る。全滅で止めたあとの戻す操作も、プレイ中には出さない。一つのシーンは作業ディレクトリの `scenes` にあり、中身は `floor` と床の半辺、キャラクターの位置、ヨー、色である。体力、クールダウン、ピッチ、ロール、カメラ、クリア色は書かない。読み込んだキャラクターの体力は 3、クールダウンは 0、ピッチとロールは 0 である。行が壊れている、数が範囲外、キャラクターが 0 人または 5 人以上のときは読み込まず、今の配置を保って失敗を出す。起動時に `arena.txt` は読まない。既にある `arena.txt` は消さない。保存は、コードの 1 対 1 の初期配置を上書きしない。編集中の初期状態に戻す操作は、今の配置をその 1 対 1 に置き換える。プレイヤーは `(0, 0, 0)`、ヨー 0 度、色 `(0.78, 0.48, 0.27)`。相手は `(0, 0, 4)`、ヨー 180 度、色 `(0.25, 0.42, 0.68)`。床の半辺は 20。この位置では攻撃のヒットボックスは届かない。保存してあるシーンは消さない。プレイ中はこの操作を出さない。ヨーをドラッグしている相手は、そのフレームだけ接近がヨーを置き換えない。相手は 0 人から 3 人までで、消せるのは末尾だけである
- キャラクターは長さ 4 の並びで、使う人数だけを変える。初期は 2 人である。先頭がプレイヤーで、パネルから消せない。足した相手の位置は `(0, 0, 4)`、ヨーは 180 度、色は `(0.25, 0.42, 0.68)`、体力は 3 である。重なっていても、その場では解消しない。各キャラクターの体力は 3 から始まる。0 のキャラクターは描かず、歩行の障害にも攻撃の対象にもしない。歩きと攻撃はプレイ中だけである。先頭以外はプレイヤーの XZ へ秒あたり 2.5 で歩き、移動中のヨーはその方向を向く。攻撃のヒットボックスがプレイヤーに重なる距離では立ち止まり、重なってから 0.5 秒待って最初の攻撃を出す。範囲を外れると待ちは消え、次に入ったときまた 0.5 秒待つ。居続けるあいだの繰り返しは 0.8 秒に一度である。開始を押したとき、その配置を開始状態として覚える。各キャラクターの体力は 3、クールダウンは 0 からプレイを始める。戦闘の途中経過は覚えない。プレイヤーの体力が 0 になるとプレイを止める。使っている相手が 1 人以上いて、その全員の体力が 0 のときも止める。相手が 0 人のときは、相手の全滅では止めない。止めたあとは自動では開始状態に戻さない。体力が 0 のキャラクターは描かない。戻す操作で開始状態の配置に戻り、編集モードに戻る。プレイは自動では再開しない。一部の相手だけが 0 のときは、その相手は止まり、プレイは続き、プレイヤーは歩ける。相手が 0 人でも、プレイヤーの体力があるあいだは歩ける
- プレイヤーのキーと相手の接近は、キャラクターに付いたコントローラーである。どちらも水平の移動ベクトルと攻撃トリガーを生成する。プレイセッションはその入力から移動と攻撃を進める。移動と攻撃は先頭の添字を特別扱いしない。カメラの注視点と全滅の判定は、先頭のキャラクターのままである
- ヒットボックスの重なりはヒットの結果を出す。体力と停止残フレームはヒットボックスの中では変えない。プレイセッションが体力を 1 減らし、当たったキャラクターのフレーム経過時間を 0 にする
- プレイ中、ビューポートにカーソルがあるとき、WASD でプレイヤーが XZ を歩く。既定の前後左右はプレイヤーのヨーで、歩行はヨーを上書きしない。切り替えるとカメラの水平ヨーになり、移動中だけヨーが移動方向を向く。動いたあとに、壁か他のキャラクターへ重なった軸の移動だけ取り消す。相手の歩行も同じ解消である。Y は変えない。Space でプレイヤーが自分のヨーの前方を一度攻撃する。次まで 0.4 秒空ける。クールダウンの最後の 0.15 秒に押した Space は覚え、0 になったフレームに一度出す。それより前の Space は捨てる。届くと他のキャラクターの体力が 1 減る。歩きながらの接触では減らない。出したフレームから 3 フレーム、判定に使ったヒットボックスを描き、そのあいだ判定も残る。出始めと終わりは 0 フレームである。空振りでも描く。同じヒットボックスは一つのキャラクターを一度だけ減らす。覚えた押下のフレームにはヒットボックスを出さない。クールダウンが残っているフレームには新しいヒットボックスは出ない。プレイヤーの体力が 0 のあいだは、WASD も Space も効かない。編集中と、プレイを止めたあとは、WASD も Space も相手の歩行も攻撃も進まない。出し済みのヒットボックスは、プレイを止めたあとも残フレームのあいだ描く。編集に戻ると消す。プレイ中、当たったキャラクターは次のフレームから 4 フレーム、移動と攻撃のフレーム経過時間が 0 になる。キーは毎フレーム読む。当たっていないキャラクターは通常のフレーム経過時間のまま進む。ヒットボックスの寿命は止まらない。クールダウンが既に 0 のときは、経過時間が 0 でも攻撃トリガーは通る。

次を入れるときは、先に ADR を `accepted` にする。それまではコードに入れない。

- DirectX 12、描画バックエンドの抽象化
- ECS、シーン階層、コンポーネント
- 物理、アニメーション、音声
- アセットパイプライン、独自 GUI

## 変更の手順

コミットしてよいか、push してよいか、PR を作ってよいかは聞かない。次の順で進める。

1. 作業する。機能や設計の変更は GitHub issue から始める。issue に書いていないものを足さない。描画 API、依存ライブラリ、ディレクトリ構成、ゲームの仕組みを変えるときは、コードの前に `docs/adr/` の ADR を `accepted` にする。ファイル名は `NNNN-kebab-title.md`。状態は `proposed` / `accepted` / `superseded`。本文は日本語。実装は、その issue と ADR の範囲だけ。
2. ブランチを切る。`main` には直接コミットしない。作業ツリーの変更を持ったまま、`cursor/` で始まる名前のブランチを作る。すでにそのブランチにいるときは、新しく切らない。
3. コミットして push する。メッセージは日本語で、なぜ変えたかが分かる一文にする。push 先は今のブランチで、`main` へは push しない。`--force` は使わない。
4. `main` 向けの PR を作る。説明は日本語で、これまでの PR に合わせ、概要、変更、確認の順に書く。概要では issue を示す。何を変えたか、Windows でどうビルドするか、この環境で実行していないならその旨を書く。issue がない文書だけの変更は、概要にその旨を書く。

## ビルド

Visual Studio 2022 と Windows 10 SDK がある Windows だけでビルドする。手順は README に従う。

Linux やクラウド VM には Windows SDK がない。そこでは構成の確認までに留め、ビルドや起動に成功したと書かない。

## コード

- `src/` — ウィンドウ、ImGui、レンダラ
- `shaders/` — HLSL
- 新しいトップレベルディレクトリは、ADR か issue が場所を決めてから作る
- 見た目とファイルの分け方は、隣のソースに合わせる。このファイルでコーディング規約を新設しない

## 言葉

ADR、issue、PR、README は日本語。識別子と CMake のターゲット名は英語のまま。文書の言葉は `docs/words.md`。
